# Các thư viện sử dụng
`pip install numpy pandas plotly matplotlib statsmodel scikit-learn pydlm pyyaml streamlit`

# Hướng dẫn chạy
Chạy cmd và cd vào thư mục chứa README.md này. Cài đặt các thư viện cần thiết và dùng lệnh
`streamlit run src/app/app.py`